export interface EmpresaMetrica {
    empresa: string
    total_leads: number
    leads_hoje: number
    leads_semana: number
    leads_mes: number
    leads_qualificados: number
    agentes_off: number
    tabelas_contabilizadas: string[]
}